[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

; Serial monitor baud rate (matches Serial.begin in main.cpp)
monitor_speed = 115200

; TinyGPS++ – fetched automatically by PlatformIO
lib_deps =
    mikalhart/TinyGPSPlus @ ^1.0.3
